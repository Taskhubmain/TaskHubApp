# ИСПРАВЛЕНИЕ ОШИБКИ "plugin is not implemented on android"

## ⚠️ КРИТИЧЕСКИ ВАЖНО!

После изменений в коде ОБЯЗАТЕЛЬНО нужна **полная пересборка** в Android Studio!

## 🔧 Быстрое решение (3 минуты)

### 1. Откройте Android Studio

```bash
cd /path/to/project
npx cap open android
```

### 2. В Android Studio выполните ВСЕ шаги по порядку:

#### Шаг 1: Остановите приложение (если запущено)
- Нажмите красный квадрат ⏹️ в панели инструментов

#### Шаг 2: Очистите проект
- Меню: **Build → Clean Project**
- Подождите завершения (статус внизу)

#### Шаг 3: ОБЯЗАТЕЛЬНО! Инвалидируйте кэш
- Меню: **File → Invalidate Caches / Restart...**
- Нажмите **"Invalidate and Restart"**
- Android Studio перезапустится (займёт ~30 сек)

#### Шаг 4: После перезапуска - пересоберите
- Меню: **Build → Rebuild Project**
- Подождите завершения (может занять 1-3 минуты)
- В панели Build внизу должно быть: `BUILD SUCCESSFUL`

#### Шаг 5: Удалите старое приложение с устройства
- **На устройстве/эмуляторе:** долгий тап → удалить
- **ИЛИ через adb:**
  ```bash
  adb uninstall com.taskhub.app
  ```

#### Шаг 6: Запустите приложение заново
- Нажмите зелёную кнопку Run ▶️
- Приложение установится и запустится

### 3. Проверьте логи

Откройте Logcat (внизу Android Studio) и введите фильтр: `MainActivity`

**✅ ДОЛЖНЫ увидеть:**
```
D/MainActivity: onCreate called
D/MainActivity: StripePaymentPlugin registered
D/StripePaymentPlugin: StripePaymentPlugin loaded
```

**❌ Если логов НЕТ** - вернитесь к шагу 2, повторите все шаги!

### 4. Тестируйте платежи

Теперь:
1. Перейдите в раздел "Кошелек"
2. Нажмите "Пополнить"
3. Введите сумму
4. Должен открыться **нативный Payment Sheet** (не браузер!)

## 🐛 Если всё ещё не работает

### Вариант А: Ручная очистка кэша Gradle

```bash
# Закройте Android Studio полностью!

cd android
./gradlew clean
rm -rf .gradle
rm -rf app/build
rm -rf build
cd ..

# Пересинхронизируйте
npx cap sync android

# Откройте снова
npx cap open android
```

Затем повторите шаги 2-6 выше.

### Вариант Б: Проверьте файлы

```bash
# Должны быть оба файла:
ls -la android/app/src/main/java/com/taskhub/app/
# MainActivity.java
# StripePaymentPlugin.java
```

Проверьте содержимое MainActivity.java:
```bash
grep "registerPlugin" android/app/src/main/java/com/taskhub/app/MainActivity.java
```

Должно вывести:
```
registerPlugin(StripePaymentPlugin.class);
```

### Вариант В: Полная переустановка

```bash
# 1. Закройте Android Studio
# 2. Очистите всё
cd android
./gradlew clean
rm -rf .gradle build app/build
cd ..

# 3. Пересоберите веб-версию
npm run build

# 4. Синхронизируйте
npx cap sync android

# 5. Откройте и пересоберите
npx cap open android
```

В Android Studio:
1. **File → Invalidate Caches / Restart**
2. После перезапуска: **Build → Rebuild Project**
3. Удалите app с устройства: `adb uninstall com.taskhub.app`
4. Run ▶️

## 📋 Checklist перед обращением в поддержку

Если ничего не помогло, соберите информацию:

- [ ] Выполнили все шаги выше по порядку
- [ ] Выполнили "Invalidate Caches / Restart"
- [ ] Удалили старое приложение с устройства
- [ ] Проверили логи Logcat - есть ли `MainActivity: StripePaymentPlugin registered`?
- [ ] Проверили наличие файлов MainActivity.java и StripePaymentPlugin.java
- [ ] Проверили Chrome DevTools (`chrome://inspect`) - какие ошибки там?
- [ ] Сохранили логи Build из Android Studio

## 🎯 Ожидаемый результат

После правильного выполнения всех шагов:

1. **В Logcat при запуске:**
   ```
   MainActivity: StripePaymentPlugin registered
   StripePaymentPlugin: StripePaymentPlugin loaded
   ```

2. **В приложении при пополнении:**
   - Нажимаете "Пополнить"
   - Открывается **нативный Payment Sheet**
   - Вводите карту `4242 4242 4242 4242`
   - Платёж проходит успешно

3. **НЕ должно быть:**
   - Открытия браузера
   - Ошибки "plugin is not implemented"
   - Ошибки в Logcat

## ⚡ Почему это происходит?

Android Studio **кэширует** скомпилированные классы. Даже после изменения MainActivity.java, без полной очистки кэша и пересборки, может использоваться старая версия кода без регистрации плагина.

**Invalidate Caches** - это единственный способ гарантированно очистить весь кэш!
